[Unit]
Description=ttyd server

[Service]
Type=simple
ExecStart=/usr/local/bin/ttyd -W -p 800 -t fontSize=16 /bin/login
#ExecStart=/usr/local/bin/ttyd -p 800 -t fontSize=16 -W ssh -p 22022 pi@localhost
Restart=always

[Install]
WantedBy=multi-user.target
